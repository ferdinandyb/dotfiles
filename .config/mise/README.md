No global `config.toml` tracked, to allow company machine to override it with
the company config. If it actually overrides in a way that doesn't make sense,
I can still overrule via `config.local.toml`.
